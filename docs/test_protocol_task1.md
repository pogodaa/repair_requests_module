# Протокол тестирования Задания 1

## Тестируемая функция
`calculate_avg_repair_time()` — расчёт среднего времени выполнения заявок.

## Тестовый сценарий
1. Загрузить данные из файла `inputDataRequests.xlsx`.
2. Отфильтровать заявки со статусом «Готова к выдаче».
3. Для заявки №3:
   - `startDate`: 2022-07-07
   - `completionDate`: 2023-01-01
   - Разница: 178 дней
4. Вычислить среднее время (всего 1 завершённая заявка → среднее = 178.0).

## Ожидаемый результат
`178.0`

## Фактический результат
`178.0` (тест `pytest` → **PASSED**)

## Среда тестирования
- ОС: Windows 10
- Python: 3.12.3
- Библиотеки: `pandas`, `openpyxl`, `pytest`

## Вывод
Функция корректно обрабатывает реальные данные. Требования ТЗ выполнены.